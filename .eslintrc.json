

{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "react/no-unescaped-entities": "off", // Disable or set to "warn" to ignore unescaped characters like apostrophes
    "@next/next/no-img-element": "warn"  // Warn instead of error for using <img> tags
  }
}